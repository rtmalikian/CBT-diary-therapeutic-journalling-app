# Environment Variables for CBT Diary

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# Database Configuration (example)
DB_HOST=localhost
DB_PORT=27017
DB_NAME=cbt_diary

# Security headers
NODE_ENV=production
PORT=3000

# Logging
LOG_LEVEL=info

# API Keys (if needed in future)
# OPENAI_API_KEY=your-openai-key-here